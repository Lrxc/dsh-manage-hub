# dsh bundle layer: mount the mcp-skills host plugin (lib/index.js via the
# package root export). The browser half is a dsh.client row discovered by the
# same entry name and served to the web UI automatically.
- insert:
    - id: mcp-skills
      name: 'dsh-mcp-skills'
